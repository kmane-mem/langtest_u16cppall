language: c

compiler:
  - gcc
  - clang
  
runtime:
  nodePool: custom__x86_64__CentOS_7
  
build:
  pre_ci:
    - echo 'true'
    
  pre_ci_boot:
    image_name: drydock/c7cppall
    image_tag: master
    pull: true
    
  ci:
    - python -V
    - docker ps
   # - docker build -t=test .
    - docker images 
   # - ls
   # - pwd
   # - ./configure && make && make install
    #- ./hello
    
  post_ci:
    - echo 'true'
   