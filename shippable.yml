language: c

compiler:
  - gcc
  - clang
services:
  - cassandra 
  #- elasticsearch 
  - memcached 
  - mongodb 
  - mysql 
  - neo4j 
  - postgres 
  - rabbitmq 
  - redis 
  - selenium
  - sqllite 
  - couchdb 
  - rethinkdb 
  - riak 
 
build:
  pre_ci:
    - echo 'true'

  ci:
    - ./configure && make && make install
    - ./hello

  post_ci:
    - echo "true"
  pre_ci_boot:
    image_name: drydock/u14cppall
    image_tag: master
    pull: true
